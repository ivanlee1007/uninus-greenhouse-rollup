{
  "name": "uninus-greenhouse-rollup-card",
  "version": "1.0.3",
  "description": "Professional responsive greenhouse roll-up curtain card for Home Assistant",
  "type": "module",
  "main": "uninus-greenhouse-rollup-card.js",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build && node --check uninus-greenhouse-rollup-card.js",
    "ci": "npm run check && git diff --check"
  },
  "keywords": [
    "home-assistant",
    "lovelace",
    "hacs",
    "greenhouse",
    "rollup"
  ],
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.6"
  }
}
